# Public Artifact Dash Guard

Blocks `Bash` commands that publish git, GitHub PR, or GitHub release text with
an en dash or em dash. Blocks `apply_patch` edits that add those characters.

Set `CODEX_PORTABLE_DISABLE_DASH_GUARD=1` to disable the guard.

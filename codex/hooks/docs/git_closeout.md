# Git Closeout Hook

On `Stop`, asks Codex to mention dirty files or unpushed commits before
finishing.

Set `CODEX_PORTABLE_DISABLE_GIT_CLOSEOUT=1` to disable closeout checks. Set
`CODEX_PORTABLE_DISABLE_CHILD_REPO_SCAN=1` to limit scanning to the current
repository.

interface:
  display_name: "Behavior Validator"
  short_description: "Validate observable behavior without reading source"
  default_prompt: "Use $behavior-validator to establish the observable contract, build an isolated workspace, and obtain fresh source-blind clause-level evidence."

policy:
  allow_implicit_invocation: true

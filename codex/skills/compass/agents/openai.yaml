interface:
  display_name: "Compass"
  short_description: "Maintain portable Codex setup"
  default_prompt: "Use $compass to update durable Codex setup in https://github.com/ariobarin/codex-portable while preserving the install boundary."

policy:
  allow_implicit_invocation: true

interface:
  display_name: "Grill Me"
  short_description: "Map the decision space, then ask one sharp question"
  default_prompt: "Use $grill-me to map the unresolved decision space, remove questions already answered by context, and stress-test my plan one consequential question at a time."

policy:
  allow_implicit_invocation: true

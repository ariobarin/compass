interface:
  display_name: "Monitor To Completion"
  short_description: "Concentrate one mechanical wait in one command"
  default_prompt: "Use $monitor-to-completion to wait for this one observable condition inside a bounded command and return a compact terminal result."

policy:
  allow_implicit_invocation: true

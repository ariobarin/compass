---
name: pr-merge-closeout
description: Use pr-review-loop to verify a named PR and merge if good. If merged, archive this thread. Otherwise, leave open and comment on the PR.
---

# PR Merge Closeout

Use `pr-review-loop` to verify the named PR and merge if good. If merged,
archive this thread. Otherwise, leave open and comment on the PR.

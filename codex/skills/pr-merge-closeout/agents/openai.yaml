interface:
  display_name: "PR Merge Closeout"
  short_description: "Verify and close out a PR"
  default_prompt: "Use $pr-merge-closeout and $pr-review-loop to verify a named PR and merge if good. If merged, archive this thread. Otherwise, leave open and comment on the PR."

policy:
  allow_implicit_invocation: true

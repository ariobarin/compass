interface:
  display_name: "Review"
  short_description: "Bias-resistant specialist review"
  default_prompt: "Use $review to run a scoped specialist review through the reviewer coordinator without poisoning context."

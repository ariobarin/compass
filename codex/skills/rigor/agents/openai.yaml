interface:
  display_name: "Rigor"
  short_description: "Demand evidence before claims"
  default_prompt: "Use $rigor to audit evidence before a consequential completion, count, or process-stop claim."

policy:
  allow_implicit_invocation: false

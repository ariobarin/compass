interface:
  display_name: "Rigor"
  short_description: "Demand evidence before claims"
  default_prompt: "Use $rigor to verify evidence before claiming success, counting results, stopping work, or fixing a failure."

policy:
  allow_implicit_invocation: true

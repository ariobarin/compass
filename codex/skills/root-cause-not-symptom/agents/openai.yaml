interface:
  display_name: "Root Cause, Not Symptom"
  short_description: "Repair the mechanism and simplify the boundary"
  default_prompt: "Use $root-cause-not-symptom to establish the causal model and owning boundary before editing, then perform a subtractive review after the repair."

policy:
  allow_implicit_invocation: true

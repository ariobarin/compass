interface:
  display_name: "Root Cause, Not Symptom"
  short_description: "Fix causes, not symptoms"
  default_prompt: "Use $root-cause-not-symptom to force root-cause-first on this bug: name the cause before any fix, and refuse to patch, suppress, swap, or declare done on a symptom."

policy:
  allow_implicit_invocation: true

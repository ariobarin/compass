interface:
  display_name: "Update Compass"
  short_description: "Refresh live setup from Compass"
  default_prompt: "Use $update-compass to refresh my live setup from reviewed Compass main."

policy:
  allow_implicit_invocation: false

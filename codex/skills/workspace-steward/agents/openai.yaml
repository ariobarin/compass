interface:
  display_name: "Workspace Steward"
  short_description: "Shape durable umbrella workspaces"
  default_prompt: "Use $workspace-steward to set up or maintain an umbrella workspace with clean main checkouts, PR worktrees, experiments, local docs, artifacts, tmp space, and cleanup rules."

policy:
  allow_implicit_invocation: true

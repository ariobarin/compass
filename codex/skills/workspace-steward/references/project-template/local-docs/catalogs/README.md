# catalogs

A catalog is the current index of active repositories, worktrees, assignments,
experiments, monitors, evidence artifacts, decisions, and checkpoints.

Keep locators and current status concise. Point to source documents instead of
copying them. Include `Last verified at` so a fresh context can judge freshness.

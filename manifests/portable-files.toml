# Human-owned Codex and Agents files that this repo is allowed to track.

[codex]
# The live target is the active Codex home: $CODEX_HOME when set, otherwise
# the default %USERPROFILE%\.codex path.
home = "$CODEX_HOME or %USERPROFILE%\\.codex"

files = [
  "AGENTS.md",
  "keybindings.json",
]

dirs = [
  "agents",
]

[agents]
# User skills install to the current user skill home, separate from the
# Codex home used for AGENTS.md, keybindings, and custom agents.
home = "$HOME\\.agents"
skills_dir = "$HOME\\.agents\\skills"

skills = [
  "action-items-to-prs",
  "benchmark-infra-reviewer",
  "benchmark-run-operator",
  "compass",
  "git-branch-resolver",
  "grill-me",
  "orchestration-controller",
  "subagent-driven-development",
  "to-prd",
  "ui-ux-pro-max",
  "using-codex-goals",
  "webmcp-eval-triage",
  "webmcp-tool-authoring",
  "webmcp-verify-tool",
  "write-a-skill",
]

[config]
review_file = "config.review.toml"
install_automatically = false
reason = "Live config.toml includes generated runtime paths, marketplaces, project trust, and MCP server wiring."

[repo_only]
# These paths are repository-root paths. The [codex] section above maps files
# from the repo's codex/ directory into the live Codex home. The [agents]
# section maps repo skills into the user Agents home.
files = [
  "AGENTS.md",
  "README.md",
]

dirs = [
  "local-docs",
  "manifests",
  "scripts",
  "workflows",
]
reason = "These files guide repository maintenance but are not installed into a live Codex or Agents home."

[local_only]
files = [
  "AGENTS.override.md",
  "auth.json",
  "history.jsonl",
  "session_index.jsonl",
  "installation_id",
  "cap_sid",
  "models_cache.json",
  ".codex-global-state.json",
]

dirs = [
  ".sandbox",
  ".sandbox-bin",
  ".sandbox-secrets",
  ".tmp",
  ".local",
  "archived_sessions",
  "automations",
  "browser",
  "cache",
  "computer-use",
  "log",
  "memories",
  "memories_extensions",
  "node_repl",
  "plugins",
  "process_manager",
  "rules",
  "sessions",
  "sqlite",
  "threads",
  "tmp",
  "vendor_imports",
  "worktrees",
]

patterns = [
  "*.sqlite",
  "*.sqlite-shm",
  "*.sqlite-wal",
  "*.log",
]
